---
import '../../static/global.css'
import '@fontsource-variable/inter'
import Layout from '@/layouts/Layout.astro'
import Navbar from '@/components/Navbar.astro'
import About from '@/sections/About.astro'
import Projects from '@/sections/Projects.astro'
import Skills from '@/sections/Skills.astro'
import Footer from '@/components/Footer.astro'

const date = new Date()
const formattedDate = new Intl.DateTimeFormat('es-ES', { dateStyle: 'medium' }).format(date)
---

<Layout title="my page">

  <Navbar />
  <div class="max-w-6xl mt-36 m-auto p-4 flex flex-col gap-20">
    <!-- Mensaje temporal sobre el rediseño del portfolio -->
    <div class="w-96 fixed top-40 right-96 bg-black rounded p-3 text-white shadow-lg shadow-slate-800 flex flex-col items-center">
      <span class="text-center">🔧 ⚠️Actualmente, este portafolio está en proceso de rediseño y refactorización 🔧</span>
      <span class="text-amber-300">¡Vuelve pronto para ver lo nuevo!</span>
      <span>{formattedDate} </span>
    </div>
    <About />
    <Skills />
    <Projects />
  </div>
  <Footer />
</Layout>
