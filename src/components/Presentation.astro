---
import GithubIcon from './svgs/GithubIcon.astro'
import LinkedinIcon from './svgs/LinkedinIcon.astro'
---

<main id="about" class="fade-in w-full flex mt-20">
  <div class="w-full sm:w-[90%] md:w-[80%] max-w-4xl m-auto p-3 flex flex-col gap-8 items-center">
    <div class="flex flex-col gap-2 text-center">
      <h1 class="text-slate-900 dark:text-zinc-100 text-4xl sm:text-5xl md:text-6xl font-bold">ELWIN ROMAN</h1>
      <h2 class="text-rose-600 text-xl sm:text-3xl font-bold">Software Engineer</h2>
    </div>

    <div class="max-w-xl flex flex-col text-center gap-3 text-sm md:text-[0.94rem] text-neutral-700 dark:text-neutral-300">
      <p class="text-balance">
        Amplios y sólidos conocimientos en desarrollo web. Capacidad de crear proyectos web que abarca la planificación, desarrollo y despliegue.
      </p>
      <p class="text-balance">
        Experiencia en la gestión ágil de proyectos. Altamente colaborativo, creativo, y sobre todo autodidacta. Comprometido con la excelencia, la innovación y el crecimiento profesional.
      </p>
    </div>

    <div class="flex flex-wrap gap-3 justify-center sm:justify-start">
      <button id="btn-github" class="btn-press bg-[#010409] hover:bg-[#171b20]">
        <GithubIcon width="18" height="18"/>
        <span>Github</span>  
      </button>

      <button id="btn-linkedin" class="btn-press bg-[#0a66c2] hover:bg-[#0d6fd1]">
        <LinkedinIcon width="18" height="18"/>
        <span>Linkedin</span>  
      </button>
    </div>  
  </div>
</main>

<style>
  @tailwind base;
  @tailwind components;
  @tailwind utilities;

  @media (min-width: 768px) {
    #about {
      height: calc(100vh - theme(spacing.20));
    }
  }
  .text-balance {
    /* text-wrap: balance; */
  }
  .btn-press {
    @apply w-[120px] shadow-lg flex justify-center items-center px-2 sm:px-3 py-1.5 sm:py-2 gap-2 focus:outline-none rounded text-xs sm:text-[0.9rem] transition-[background] duration-300 text-[#f6f8fa] fill-[#f6f8fa];
  }
</style>

<script>
  const btnGithub = document.getElementById('btn-github')
  const btnLinkedin = document.getElementById('btn-linkedin')
  
  btnGithub.addEventListener('click', (e) => {
    e.preventDefault()
    window.open('https://www.github.com/elwinroman', '_blank')
  })

  btnLinkedin.addEventListener('click', (e) => {
    e.preventDefault()
    window.open('https://www.linkedin.com/in/elwinroman', '_blank')
  })
</script>