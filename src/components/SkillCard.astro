---
const { path, alt } = Astro.props
const size = '60'
---

<div class="flex flex-col justify-center items-center gap-2 shadow-lg p-2 md:p-4 bg-gray-50 dark:bg-slate-700 border border-gray-200 dark:border-slate-800 rounded text-gray-600 dark:text-gray-300">
  <img src={path} width={size} height={size} alt={alt} class="max-w-none">
  <span class="text-sm">
    <slot />
  </span>
</div>